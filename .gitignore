# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/

# tooling / local state (not part of the library)
.claude/
.swarmdo/
CLAUDE.md
.DS_Store

# generated artifacts (rebuild with tools/build_preview.py)
/preview.html
track.json
track.csv

# quickstart GIF: rendered by VHS in CI and published to the Pages site, never
# committed (README points at the Pages URL). Rebuild locally: vhs docs/quickstart.tape
/docs/quickstart.gif
